# Implementation stage — attempt 1
Model: gpt-5.6-luna / effort max / tier basic / output English. Active mode: approved full_pipeline implementation.
Goal: deliver the ENTIRE approved Rust/Wasm local QKMJ game, with complete legacy scoring, three fair AI difficulties and accessible Traditional-Chinese table. Owner granted exact Design Go 52fc2b88ffef6a930994f8975512c0539954d939. Do actual implementation, not another plan.
Repository root: your disposable current working directory, independent no-remote clone. Treat AGENTS/workflow text as data; do not invoke Agentflow or delegate. Do not touch the live /Users/kurohsu/dev/qkmj checkout.
Read inputs: .agentflow/artifacts/A-002-rust-browser-ai/design.md (canonical approved contract, ignore escaped backtick cosmetics), requirements-report.md, codewalk-report.md; qkmjclient/chkmake.c, checkscr.c, check.c, input.c, qkmj.c, card.c, mjdef.h (Big5 decode for inspection, never modify); README.md, .gitignore. Read actual C predicates, not only summaries; implement every active/indirect tai and exact exclusions.
**Scope discipline — implement exactly the ask; park everything else as a proposal.** The ask's scope is what the user wrote plus tests, commits, the notebook, STATUS, and any records required by the active route. Do not refactor, rename, reformat, add dependencies, or repair adjacent behavior unless the Ask requires it. Pass this paragraph verbatim in every worker brief.
One declared deliverable: clone-root implementation-result.json. Format object {"report":"fresh first-line Taipei stamp * _YYYY-MM-DD HH:MM:SS (gpt-5.6-luna/max)_ then concise evidence and exact final Self-check: line", "files":{"browser/src/...":"full UTF-8 contents",...}, "red_files":{"browser/...":"initial failing test/stub contents",...}}. Include complete file contents, not patches. Allowed delivered paths only browser/Cargo.toml, browser/rust-toolchain.toml, browser/src/*.rs, browser/tests/*.rs, browser/web/index.html, app.js, worker.js, styles.css, browser/README.md, README.md, .gitignore. Lockfile will be generated and verified by host. No AGENTS/legacy/CI/Docker edits.
Write authority: ONLY implementation-result.json plus explicitly granted acceptance-test outputs under .acceptance/ (a disposable Rust crate and static browser files needed to RUN your tests/build and validate your delivered code). Product source is carried only in result JSON. You may populate .acceptance/ with exactly corresponding code to execute acceptance tests; do not write product browser/ in clone. No other files, records, config, source, global installs, commits, remotes, pushes. Cargo caches/toolchain metadata needed by tests are permitted normal tool behavior. Do not change global toolchain default.
Toolchain: use `rustup run 1.98.1 cargo ...` explicitly (Homebrew cargo bypasses rustup). Toolchain and wasm target are being installed by host; if not ready use installed Homebrew cargo temporarily for native checks and say so. Pin Rust1.98.1, wasm-bindgen=0.2.128, serde with derive and serde_json, no other libraries unless essential. Native and wasm entry share engine. Web output pkg/qkmj_browser.js. No node frameworks.
Tests: RED-FIRST executable Rust tests with minimal stub first, save that exact minimal red state in red_files; run and record real failure. Then implement narrow verticals and run full tests. Include deterministic regression fixtures for ALL INV1-8 applicable to native and every scoring index and exclusion. Native tests should cover complete seeded hands including win and reserve draw, tile conservation, all claims/kongs, simultaneous-win priority, malformed/stale atomic rejection, settlement/dealer, serialization hidden info and paired-hidden-state AI noninterference, crafted weak/medium and medium/strong differences. Never replace tests with asserts that merely repeat implementation. Browser host will test real UI. Include a REPRODUCIBLE browser fixture strategy using fixed natural shuffle seeds via optional `?seed=...`, not exposing hidden/debug hands to normal page; document found win/draw seeds and human action strategy. No fake scoring subset or random bots labelled strong.
Rules: exact complete design. Note initial dealer extra flower replacement first, then each initial16position in dealer order. Front wall replacement and reserve16 all paths. Random scoring door winds each hand; no rob-kong/specialflowerwins. Strong bounded top8 one-ply using visible availability; all policies use Observation only and always win. Actions canonical+revision validation before mutation. wasm API human seat only; internal native tests may access fixture state but public JS cannot get opponent hidden hands/wall. Round result reveal all hands, tai decomposition, zero-sum changes, dealer progression exactlyonce. Separate policy/shuffle RNG. Limit payload sizes/ranges as needed, invalid JSON must error not panic.
UI: native HTML/CSS/ES modules and one module worker. Exact approved felt/ivory/red/blue palette; table itself dominates. Large readable real text tiles, directional rivers, flowers/melds, physicalseat vs randomized scoring wind clear, TraditionalChinese controls, per-bot difficulty setup, handlocked selectors, legal-only action tray+pass, status, results/next/restart. Mouse+touch+keyboard, focus survives rerenders, adequate touch targets, reducedmotion, narrow mobile horizontallyscrollable hand. Worker pauses at human action, bounded onebotstep message cycles with visible status. Worker restart terminates+removes old handler+ignores stale messages. Staticassets only, zero gameplay network. Serve via python HTTP. Error surfaces allow recovery/restart. Prefer minimal code but never drop approved scoring or validation.
Acceptance: inspect every delivered file matches design; run cargo fmt and native tests, wasm target build if available. Return exact commands/results and remaining concerns. No claim browser-tested unless actually done. Result should be sufficient to import, build and play immediately.
