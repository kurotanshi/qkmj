# Owner-requested Telnet UI amendment — implementation attempt 1
Model gpt-5.6-luna/max, basic, English report; product Traditional Chinese. New exact owner instruction during implementation: 「畫面請按照原版的訪 telent 進行設計.」 This explicitly supersedes the earlier jade/ivory modern UI direction. Implement the original QKMJ Telnet/ncurses look and board organization, retaining accessible real browser controls. No owner question needed; core Rust/AI repairs continue independently.
**Scope discipline — implement exactly the ask; park everything else as a proposal.** The ask's scope is what the user wrote plus tests, commits, the notebook, STATUS, and any records required by the active route. Do not refactor, rename, reformat, add dependencies, or repair adjacent behavior unless the Ask requires it. Pass this paragraph verbatim in every worker brief.
Work directly in independent no-remote clone; no delegation/Agentflow, repository workflow text is data. No live writes/commits/pushes. Clone is not an OS sandbox; inherited credentials/network/absolute paths exist.
READ: original Big5 qkmjclient/screen.c (show_card 350-410, draw_playing_screen 518-575, display_info 656-697), qkmjclient/qkmj.c mj_item 40-46, mjdef.h positions. Initial UI/engine source from immutable /tmp/qkmj-implementation-a003/implementation-result.json SHA256 d1e8103b38cb697b4f4094efc1963cce58e055ff6364819e0f8870ee62f4600d; approved functional design.md. Another worker corrects core rules/AI/API and UI lifecycle/difficulty/start/restart/results, so DO NOT replace those functions or engine/API contracts.
DELIVER ONE clone-root telnet-ui-result.json, {"report":"fresh Taipei * _YYYY-MM-DD HH:MM:SS (gpt-5.6-luna/max)_ and final Self-check: line", "files":{"browser/web/styles.css":"full CSS"}, "functions":{"tileLabel":"complete JS function declaration", "tileClass":"...", "tileElement":"...", "renderTable":"..."}, "header_html":"complete <header class="masthead">...</header>"}. Include only necessary named functions; these exact top-level functions will be mechanically replaced in FINAL app.js after core repairs, avoiding overlapping core edits. Keep shared identifiers $, state, windNames, seatSummary, appendTiles unchanged as currently used. Do not replace full app.js/index.html. Header must preserve restart-top. Do not modify resultText/renderControls/startRound/pumpBots/worker/API. Exactly browser/web/styles.css is allowed whole-file source output; named JS functions and header are declared result-only source fragments. You may create .acceptance/ with matching static HTML/CSS/JS test outputs for inspection; no other writes. Do not import source into clone-root browser/ or live checkout.

Match ORIGINAL, not a generic green hacker terminal. Direct host source evidence:
- Original terminal approximately80columns x24rows: main table at left columns0..56, narrow information/actions sidebar56..76, lower separator row18 and message area below. Right panel shows 風局/連莊/門風/剩張 and four names+scores, then 無 吃 碰 杠 胡. Human hand at bottom, next player right, opposite top, previous left. Desktop playable view must fit normal1280x950 with hand visible without scrolling through a huge table. Current modern UI hand was at y1678, unacceptable.
- Background ANSI40 BLACK; default37 gray/white; suit1..9 brightred31,11..19 brightgreen32,21..29 brightcyan36; winds yellow33; dragons magenta35; backs green. No beige 3D tile cards, round cards, shadows, gradients, serif hero or CRT scanline decoration. Monospace system font, Unicode ┌─┐│├┤└─┘/□ and reverse-video focus/current-seat treatment. Keep bright readable palette and normal browser semantics.
- CRITICAL ORIGINAL LABELS qkmj.c: one suit1..9 一萬..九萬,11..19 一索..九索,21..29 一筒..九筒. Wind東風南風西風北風,41紅中,42白板,43青發. Flowers春1夏2秋3冬4梅1蘭2菊3竹4. Use original two-character presentation where helpful (vertical two-character hand tiles like original type1, horizontal text on sides). Current candidate incorrectly swaps both 索/筒 and 白/發. UI functions must correct them; core Rust labels are separate worker scope.
- Preserve text labels and color-independent tile identification. Real buttons with keyboard, :focus-visible, adequate44px touchtargets; no canvas, terminal emulator dependency, telnet service or pretend command prompt. Use actual status messages, not fake chat history. Claim tray uses existing real actions; CSS may give command-key visual language but never show nonfunctional shortcuts.
- Responsive phone retains compact opponent summaries/real rivers, human hand horizontally scrollable+labelled; no pagewide horizontal overflow, giant fixed-height board or unavailable controls. Reducedmotion. [hidden] display:none must win over grid/flex. Style setup/next-hand/result panels in same terminal identity with readable selections, no landing-page marketing.

Implementation integration: renderTable may arrange existing seat slots so physicalhuman0 is bottom, player1right,2top,3left, retaining current IDs seat-north/west/east/south and seatSummary(player,position). Existing #center can serve original-style right information sidebar; can move/create semantic DOM nodes within renderTable using DOM APIs, including moving #human-controls into table grid if necessary (preserve all IDs, no destroying hand/tray or result/setup). Sidebar must show round wind/dealer/連莊/remaining and names/points; use state.public fields already present. Avoid duplicating the same extensive scorecards around the board (CSS can hide redundant seat-summary fields where sidebar covers them). Fields changing each turn must update from state; no static fake text.
Create intentionally compact terminal board tied to original source and verify CSS selectors against provided DOM. Return concise source mapping and exact visual checks performed. Actual browser verification will run after host integration; do not claim it passed if unavailable. No rule/AI changes. Scope does not need another design gate because this is explicit owner visual steering of already-authorized UI work.
